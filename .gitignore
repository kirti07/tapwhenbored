node_modules/
dist/
.vite/

# Vite loads these; only VITE_-prefixed vars reach the browser.
# .env.example is committed as the template.
.env
.env.local
.env*.local

.DS_Store
.architecture.md

# Playwright
test-results/
playwright-report/

# The UX research: personas, prototypes, screenshots. Read from, never built
# from, and large. Kept out of the repo rather than out of the working tree.
research/
